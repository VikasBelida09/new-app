import React from "react";
import { View, Text } from "react-native";

const Helper = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Helper Component</Text>
    </View>
  );
};

export default Helper;
